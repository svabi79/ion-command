# Unreal plugins

Project-local Unreal plugins may be added here when a module needs independent
packaging. Runtime modules currently live below `Source` to keep First Light
build and editor automation simple.

