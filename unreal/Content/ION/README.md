# Generated Unreal assets

Assets below `/Game/ION` are generated by the scripts in `unreal/Scripts`.
`tools/run-editor.ps1` first builds the deterministic starfield source, then
imports the documented NASA globe sources and generates materials, data assets,
and the command-deck level.
